#pragma once

/*

Divide into grid with given size:

Then create array of with size of the grid
containing a pair of DynamicLinkedList.

Then when checking collision only check collisions
between the ones

*/
class Collision
{
private:
    /* data */
public:
    Collision(/* args */);
    ~Collision();
};

